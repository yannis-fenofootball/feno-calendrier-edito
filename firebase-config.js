// Configuration Firebase du calendrier Feno.
// Remplace les valeurs ci-dessous par celles de ton projet Firebase
// (Console Firebase > Paramètres du projet > Tes applications > icône Web).
// Ces valeurs ne sont PAS secrètes : la sécurité réelle vient des règles
// Firestore (firestore.rules), pas de la confidentialité de ce fichier.
window.FENO_FIREBASE_CONFIG = {
  apiKey: "REPLACE_ME",
  authDomain: "REPLACE_ME.firebaseapp.com",
  projectId: "REPLACE_ME",
  storageBucket: "REPLACE_ME.appspot.com",
  messagingSenderId: "REPLACE_ME",
  appId: "REPLACE_ME"
};
